{
    "file info": {
        "description": "Global Secrets - every site can see these",
        "file format": "JSON standard with variables in unnamed base dictionary (JSON Object)",
        "use format": "This file is loaded into the dictionary sub-key: 'secret'",
        "note": "Do not put secrets in github stored files! Mask secrets with {{ secret.<var> }}. Secrets go into ~/.secrets/secrets.json"
    },
	"SecretVar": "{{ secrets.SecretVar }}"
}
